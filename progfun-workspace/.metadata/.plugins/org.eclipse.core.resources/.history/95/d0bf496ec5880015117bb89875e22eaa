package week2

object lecture2 {
  def prod(f: Int => Int)(a: Int, b:Int): Int =
  	if (a > b) 1 else f(a) * prod(f)(a + 1, b)
  	
  prod(x => x * x * x) (1, 3)
  
  def fact(n: Int): Int = prod(x => x) (1, n)
  
}